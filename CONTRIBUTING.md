// Contribution guidelines
// Branching model: feature/*, fix/*, chore/*; PRs to main with at least one reviewer
// Coding style: Python (PEP8), JS (ESLint), Arduino (clear pin constants)
// Hardware safety rules: no live tests without two-person sign-off and E-stop accessible
// How to run tests and CI expectations
