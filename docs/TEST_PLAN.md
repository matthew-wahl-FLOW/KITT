// Test plan: unit tests, integration tests, hardware-in-the-loop tests
// Simulation steps to validate orchestrator logic before live runs
// Acceptance criteria for each phase (broker + jmri, sensors, firmware, UI)
